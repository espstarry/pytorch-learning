{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Current Exercise as Module",
            "type": "debugpy",
            "request": "launch",
            "module": "exercises.${fileBasenameNoExtension}",
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal"
        }
    ]
}
