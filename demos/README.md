# Demos

这里的每个目录都是一个独立的最小模型例子。

推荐阅读顺序：

1. `template_000`：最小 CNN 和训练四步
2. `vision/`：LeNet -> AlexNet -> VGG -> ResNet
3. `sequence/`：RNN -> LSTM -> Transformer
4. `nlp/`：Transformer -> BERT
5. 其他目录按兴趣继续

新模型只需要创建一个目录，优先写 `model.py` 和 `train.py`。不要为了统一而提前抽象公共工具。
